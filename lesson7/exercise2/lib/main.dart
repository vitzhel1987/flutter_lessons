/*
Создайте InheritedWidget, который будет хранить значение состояния (например, счетчик).
Создайте два дочерних виджета:
Один должен отображать текущее значение состояния (например, текущий счетчик).
Другой — увеличивать значение состояния на 1 при нажатии на кнопку.
 */

import 'package:flutter/material.dart';
import 'my_app.dart';

void main() {
  runApp(const MyApp());
}


